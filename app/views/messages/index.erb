<h1>Messages</h1>

<% @messages.each do |message| %>
  <h3><%= message.title %></h3>
  <p><%= message.description %></p>
  <p><%= link_to "Edit Message", edit_message_path(message) %></p>
  <p><%= link_to "Delete Message", message, method: :delete %></p>
<% end %>